name: Eco-Audit & Quality Check

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  eco-quality-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Instalar Dependencias
        run: |
          pip install codecarbon
          # Si tienes más dependencias: pip install -r requirements.txt

      - name: 🟢 Ejecutar Auditoría Reutilizable
        # AQUI ESTÁ EL CAMBIO CLAVE:
        # Ejecutamos el runner y le pasamos tu proyecto como argumento
        run: |
          python emissions_runner.py main.py
          
          echo "----------------------------------------"
          echo "Verificando generación de reporte..."
          if [ -f emissions.csv ]; then
            cat emissions.csv
          else
            echo "❌ Error: No se generó el archivo emissions.csv"
            exit 1
          fi

      - name: 📤 Subir Reporte de Emisiones como Artefacto
        uses: actions/upload-artifact@v4
        with:
          name: reporte-emisiones
          path: emissions.csv

      - name: 🔍 Análisis de Calidad con SonarQube
        # Nota: Para que esto funcione, necesitas configurar SONAR_TOKEN y SONAR_HOST_URL en los Secrets de GitHub
        # CORRECCIÓN: Cambiamos a la acción oficial 'sonarqube-scan-action'
        uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}